export declare type Size = {
    height: number;
    width: number;
};
export declare type ParseSizeFn = (rawSize?: string) => Size;
export declare type ParseSizeFactory = (minSize: number, maxSize: number) => ParseSizeFn;
export declare const parseSizeFactory: ParseSizeFactory;
