import { HashFn } from './hashing';
export declare class Hash<T> {
    length: number;
    values: T[];
    hashFn: HashFn;
    constructor(values?: T[], hashFn?: HashFn);
    /**
     * Retrieves one of the constructed values by hashing the provided key.
     * Uniformity of returned values is determined by the hashing function.
     * @param key
     */
    get(key: string): T;
    private indexFor;
}
